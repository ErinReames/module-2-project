<h1>Available Users</h1>
<%= link_to('Create New User', new_user_path) %>

<ul><% @users.each do |user| %>
   <li><%= link_to(user.username, user_path(user)) %> (<%= pluralize(user.upcoming_appointments.count, "upcoming appointment") %>) </li> 
<% end %></ul>